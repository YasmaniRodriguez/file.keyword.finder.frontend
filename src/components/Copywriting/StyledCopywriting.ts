import styled from "@emotion/styled";
import { Box } from "@mui/material";

const StyledCopywriting = styled(Box)`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 10px;
  width: 100%;
  min-height: 15vh;
`;

export default StyledCopywriting;
